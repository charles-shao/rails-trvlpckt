<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :img_url %><br />
    <%= f.text_field :img_url %>
  </div>
  <div class="field">
    <%= f.label :lat %><br />
    <%= f.text_field :lat %>
  </div>
  <div class="field">
    <%= f.label :lng %><br />
    <%= f.text_field :lng %>
  </div>

  <%= f.fields_for :comment do |builder| %>
    <div class="field">
      <%= builder.label :author %><br />
      <%= builder.text_field :author %><br />
    </div>
    <div class="field">
      <%= builder.label :location %><br />
      <%= builder.text_field :location %><br />
    </div>
    <div class="field">
      <%= builder.label :message %><br />
      <%= builder.text_field :message %><br />
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
